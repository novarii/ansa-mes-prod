# feature-work-orders

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build feature-work-orders` to build the library.

## Running unit tests

Run `nx test feature-work-orders` to execute the unit tests via [Jest](https://jestjs.io).
