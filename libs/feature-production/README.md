# feature-production

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build feature-production` to build the library.

## Running unit tests

Run `nx test feature-production` to execute the unit tests via [Jest](https://jestjs.io).
